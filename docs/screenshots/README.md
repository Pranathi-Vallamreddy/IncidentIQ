# Screenshots

These are **placeholder** images referenced by the root `README.md`. Replace each
with a real capture from the running app (keep the same filenames so the README
links keep working):

| File | Capture |
|---|---|
| `dashboard.png` | Dashboard / Overview (full page) |
| `incident-detail.png` | An incident's detail page (e.g. `/incidents/INC-4807`) |
| `incidents.png` | Incident Explorer table |
| `analytics.png` | Analytics page |

Suggested width: ~1600px, dark theme, PNG.
